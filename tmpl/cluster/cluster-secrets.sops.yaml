# yamllint disable
apiVersion: v1
kind: Secret
metadata:
    name: cluster-secrets
    namespace: flux-system
stringData:
    SECRET_DOMAIN: ${BOOTSTRAP_CLOUDFLARE_DOMAIN}
    SECRET_CLOUDFLARE_EMAIL: ${BOOTSTRAP_CLOUDFLARE_EMAIL}
    SECRET_VPN_GATEWAY_PORT: ${BOOTSTRAP_SECRET_VPN_GATEWAY_PORT}
    NETWORK_MANAGEMENT_CIDR: ${BOOTSTRAP_NETWORK_MANAGEMENT_CIDR}
    SECRET_VPN_FORWARDED_PORT_1: ${BOOTSTRAP_SECRET_VPN_FORWARDED_PORT_1}
